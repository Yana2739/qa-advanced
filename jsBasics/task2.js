const firstName = "Anna";
const secondName = "Yana";

// Конкатенація
const greetingConcat = "Hello, " + firstName + " and " + secondName + "!";
console.log(greetingConcat);

// Шаблонний рядок
const greetingTemplate = `Hello, ${firstName} and ${secondName}!`;
console.log(greetingTemplate);